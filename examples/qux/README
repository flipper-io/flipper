- Follow these steps to run this module. -

Ensure you have installed the latest version of the Flipper SDK.

Run 'make' from this directory to build the control program.

The usage of this program is: ./qux [on | off]

Change directories into driver/device and run 'make' there to build the module.

In that same directory, run 'make load' to load the module to the device.

Connect an Led to CPU pin 7 (labeled as IO09 on the board).

Go back to the root directory of this project and run the control program.

Basque in accomplishment.
