#define __private_include__

#include <flipper/flipper.h>

#include <fmr/fmr.h>

const void * const objects[] = { &button, &flash, &self, &host, &device, &fs, &i2c, &io, &led, &pwm, &sam, &spi, &timer, &usart, &usart1, &dbgu, &usb, &wifi, &fdl };