#define __private_include__

#include <flipper/flipper.h>

#include <fmr/fmr.h>

const void * const objects[] = { &self, &host, &device, &button, &at45, &fs, &i2c, &io, &led, &pwm, &sam, &spi, &timer, &usart, &usart1, &dbgu, &usb, &wifi, &fdl };