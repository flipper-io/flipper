/* Polling interface for deteching Flipper devices over USB. */

#include <flipper/core.h>

/* Returns a list of Flipper devices currently attached to this computer. */

struct _lf_device *lf_poll(void) {
	return NULL;
}
