# Libflipper build script.

def configure(cfg):
    pass

def build(bld):
    # Build libflipper.
    _env = bld.all_envs['platform-posix'].derive()
    bld.shlib(source = bld.path.ant_glob('src/**'), target = 'flipper', use = 'platform-posix' + ' modules-posix', env = _env)
