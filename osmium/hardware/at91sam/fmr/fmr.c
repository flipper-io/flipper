#define __private_include__

#include <flipper/flipper.h>

#include <fmr/fmr.h>

#include <modules/bme280/bme280.h>

const void * const objects[] = { &button, &flash, &host, &self, &device, &fs, &i2c, &io, &led, &pwm, &sam, &spi, &timer, &usart, &usart1, &dbgu, &usb, &wifi, &fdl, &bme280 };